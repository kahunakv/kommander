name: Run Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  Kommander-Tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GIT_BRANCH: ${{ github.head_ref || github.ref_name }}
      GIT_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5
        with:
          fetch-depth: 2
      - name: Install .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x
      - name: Check determinism boundary
        # A new direct wall-clock or thread-pool read in the consensus path makes a simulation
        # run unreplayable, and the failure is silent: the run still passes. Fail the build here
        # instead, before the run stops being reproducible.
        run: bash scripts/check-determinism-boundary.sh
      - name: Run Tests
        run: (cd Kommander.Tests && dotnet test -l "console;verbosity=detailed" --filter "Category!=Stress" --blame-hang --blame-hang-timeout 120s)
